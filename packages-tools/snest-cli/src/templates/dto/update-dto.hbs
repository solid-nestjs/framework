{{#if (eq apiType 'hybrid')}}
import { PartialType, SolidInput } from '{{solidBundle}}';
{{else if (eq apiType 'rest')}}
import { PartialType } from '@nestjs/swagger';
import { SolidInput } from '{{solidBundle}}';
{{else if (eq apiType 'graphql')}}
import { PartialType } from '@nestjs/graphql';
import { SolidInput } from '{{solidBundle}}';
{{/if}}
import { Create{{pascalCase  entityName}}Dto } from './create-{{kebabCase entityName}}.dto';

@SolidInput()
export class Update{{pascalCase entityName}}Dto extends PartialType(Create{{pascalCase entityName}}Dto) {}